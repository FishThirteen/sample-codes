:let i = 125
:g/./s/$/\=printf(" %d", i)/ | let i = i + i 

1. let 是定义变量和给变量赋值
2. g (g/<pattern>/exec)命令为查找符合某个pattern的行,并执行相应的命令
3. \=funciton() 是在替换语句中展开(调用)函数的方法
4. printf函数的用法类似c语言大的用法
5. | 用于执行多条语句, 不是管道
6. 替换命令(s) 最后的/最好加上，本例中如果不加这个/后面的 | 符号将不能起到链接两个命令的作用
