apply plugin: 'java'

repositories {
    mavenCentral()
    maven { url ("http://repo.spring.io/libs-snapshot") }
}

dependencies {
    compile 'org.springframework:spring-context:3.2.5.RELEASE'
    
    
}
